# Test basic list functionality with help command
exec gh-comment list --help
stdout 'List all comments on a pull request sorted by newest first'
stdout 'Usage:'
! stderr .

# Test root help command
exec gh-comment --help
stdout 'gh-comment provides professional-grade tools'
stdout 'Available Commands:'
! stderr .

-- .github/config.yml --
# Mock GitHub CLI config
